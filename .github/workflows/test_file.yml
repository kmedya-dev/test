name: Test File

on:
  push:

jobs:
  check-size:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run download check
        run: |
          echo "Running test download..."
          python3 download.py
