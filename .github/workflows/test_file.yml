name: Test File

on:
  push:

jobs:
  check-size:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run <file_name> check
        run: |
          echo "Running <file_name>..."
          python3 
